<body>
  <h1>Books</h1>

  <table class="hoge">
    <theader class="fuga">
      <th>カラム名1</th>
      <th>カラム名2</th>
      <th>空欄1</th>
      <th>空欄2</th>
      <th>空欄3</th>
    </theader>

    <% @booklist.each do |l| %>
      <tr class="piyo">
        <td><%= l.title %></td>
        <td><%= l.body %></td>
        <td><%= link_to 'Show', :books %></td>
        <td><%= link_to 'Edit', :books %></td>
        <td><%= link_to 'Destroy', :books %></td>
      </tr>
    <% end %>
  </table>

  <div class="CrateBook">
    <h1>New book</h1>
    <p>エラー処理はEditと共有すること<br>@bookが上と競合するので解決策を考えよう</p>

    <%= form_with model: @booknew, url: '/books', method: :post do |f| %>
        <h4>タイトル</h4>
        <%= f.text_field :title %>

        <h4>本文</h4>
        <%= f.text_area :body %>

        <%= f.submit '投稿' %>
    <% end %>

    <%= @booklist %>
    </div>
</body>
